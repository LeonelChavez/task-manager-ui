import { TaskRepositoryImpl } from './task-repository-impl';

describe('TaskRepositoryImpl', () => {
  it('should create an instance', () => {
    expect(new TaskRepositoryImpl()).toBeTruthy();
  });
});
